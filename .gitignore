node_modules/
dist/
coverage/
*.tsbuildinfo
.pnpm-store/

# Internal planning documents — kept local for maintainers, not published
/TallyMCP_Business_Requirements.md
/TallyMCP_Detailed_Implementation_Plan.md
/TallyMCP_Implementation_Plan_v2.1_Errata.md
/TallyMCP_Implementation_Plan_ReadOnly_Submission.md
/CURSOR_START_WINDOWS.md

# Agentic workflow artifacts (specs + plans authored by Claude/superpowers
# subagents during development) — internal-only, not for the public repo
/docs/superpowers/

# IDE / agent session state
/.claude/
/.cursor/*
!/.cursor/rules/
/.vscode/
/.idea/
/.superpowers/

# Local debug / probe artifacts
/tallyhttp.log
/tdlfunc.log
*.log
/tally-state-*.png
/tally-probe-*.xml

# Phase 3 installer build artifacts (Task 6+7 populate staging/; Task 8 writes dist-installer/)
installer/staging/
apps/configurator/dist-installer/

# Code signing — never commit private cert material
*.pfx
*.p12

# OM AI Reviewer — never commit the room config (contains a secret token),
# its review/communication artifacts, or the reviewer rule files.
/.omai-review/
/.omai-review.json
/ai-review/
/.cursor/rules/omai-reviewer.mdc
/.cursor/rules/tallymcp-review.mdc

# Agent / swarm runtime state (ruflo / claude-flow) — local only
/.swarm/

# AI-agent guidance — kept local, not committed (repo tracks product code + README)
/CLAUDE.md
/AGENTS.md

# Generated report output + release build artifacts — never commit
/output/
/release-test/

# Stray shell-glob artifacts (guard against accidental brace-expansion files)
/({,+
/{,+
/b.backupPath
