node_modules/
dist/
coverage/
*.tsbuildinfo
.pnpm-store/

# Internal planning documents — kept local for maintainers, not published
/TallyMCP_Business_Requirements.md
/TallyMCP_Detailed_Implementation_Plan.md
/TallyMCP_Implementation_Plan_v2.1_Errata.md
/TallyMCP_Implementation_Plan_ReadOnly_Submission.md
/CURSOR_START_WINDOWS.md

# Agentic workflow artifacts (specs + plans authored during development)
# — internal-only, not for the public repo
/docs/superpowers/

# IDE / agent session state + AI tooling rules (not part of the product)
/.claude/
/.cursor/
/.vscode/
/.idea/
/.superpowers/

# AI review / communication artifacts — never committed
/ai-review/
/.omai-review/
/.omai-review.json

# Agent / swarm runtime state (ruflo / claude-flow) — local only
/.swarm/

# AI-agent guidance — kept local (repo tracks product code + README)
/CLAUDE.md
/AGENTS.md

# Local debug / probe artifacts
/tallyhttp.log
/tdlfunc.log
*.log
/tally-state-*.png
/tally-probe-*.xml

# Phase 3 installer build artifacts
installer/staging/
apps/configurator/dist-installer/

# Code signing — never commit private cert material
*.pfx
*.p12

# Generated report output + release build artifacts — never commit
/output/
/release-test/

# Live-Tally feature sample pack — REAL CLIENT DATA, never commit.
# (Generated by apps/mcp-server/scripts/run-all-features.ts against a live
#  TallyPrime. The tracked samples/*.xml fixtures stay; this folder does not.)
/samples/feature-pack/
/samples/*.xlsx

# ICAI / AICA Level-2 Capstone submission package — local only
/icai-submission/

# Stray shell-glob artifacts (guard against accidental brace-expansion files)
/({,+
/{,+
/{
/b.backupPath
