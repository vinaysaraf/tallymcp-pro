import { describe, it, expect } from "vitest";

describe("@tallymcp/configurator", () => {
  it("workspace loads", () => {
    // Sanity: vitest can find the package and run a test
    expect(1 + 1).toBe(2);
  });
});
